SUPABASE_URL=https://mynxlubykylncinttggu.supabase.co
SUPABASE_ANON_KEY=
IBGE_API_URL=https://servicodados.ibge.gov.br/api/v1/localidades/municipios
HTTP_TIMEOUT_SECONDS=30
FUZZY_CUTOFF=0.82

# Optional: direct submission token (used when --access-token is omitted)
ACCESS_TOKEN=

# Optional auth flow (used when ACCESS_TOKEN and --access-token are omitted):
SUPABASE_EMAIL=
SUPABASE_PASSWORD=
